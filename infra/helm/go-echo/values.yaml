replicaCount: 1

image:
  repository: ghcr.io/kiukairor/go-echo
  pullPolicy: IfNotPresent
  tag: "sha-611d1fe"

imagePullSecrets:
  - name: ghcr-secret

service:
  type: ClusterIP
  port: 8080

env:
  PORT: "8080"

resources:
  requests:
    memory: "64Mi"
    cpu: "50m"
  limits:
    memory: "128Mi"
    cpu: "200m"
